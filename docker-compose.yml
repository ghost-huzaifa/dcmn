services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dcmn
    ports:
      - "5433:5432"
    volumes:
      - dcmn_pgdata:/var/lib/postgresql/data

volumes:
  dcmn_pgdata:
